#ifndef STACK_H
#define STACK_H

/*
class BookStack {
public:
    BookStack();
    void add(const books& new_book);
protected:
    //vector<books*> bstack; //since books now disappear when caught, no need for vector to hold books themselves, only booktype
    vector<int> bstack;
    //int height;
    size_t bookcount;
    friend class books;
    friend class FallingObject;
*/

#endif // STACK_H
